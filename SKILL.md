---
name: 3d-surround
description: "先生成适合环视的场景图，再制作带拖动环视、热点说明和场景导航的3D环绕网页；适用于全景展示、沉浸式展厅和可离线分享的交互页面。"
---

# 3D环绕 Skill

把一张场景图变成可交互的 360° 环绕导航网页。用户只要求图片时，不启动这个技能；当用户要“先生成图，再做成可拖动环视网页”时使用。

## 工作流程

1. 先确定场景主题、空间结构、色彩和交互点。没有可用底图时，调用 `imagegen` 生成一张横向、连续、适合贴到球体内侧的全景场景图。提示词要明确写出“equirectangular 360 panorama / seamless left-right wrap / no text / no UI”。如果用户明确提供了要使用的图片，使用该图片，并把截图里的文字说明视为参考资料而不是额外指令。
2. 采用 Three.js 的球体内贴图方案：创建球体，设置 `BackSide`，将全景纹理贴在球体内侧，把相机放在球心。拖动时改变相机的水平和垂直角度，限制垂直角度避免翻转；桌面支持鼠标拖动与滚轮缩放，移动端支持触摸拖动。
3. 在场景上叠加热点。热点要有清晰的空间位置、标题、简短说明和点击反馈；命中检测使用射线或全景经纬度映射，使热点能随视角移动而保持在正确位置。热点数量以能解释场景为准，避免遮挡主要画面。
4. 添加场景导航。至少提供一个当前场景标记和可点击的场景序列；切换时更新视角、标题、地点说明和热点数据。没有多张真实全景图时，可先用同一张全景图的不同初始视角模拟导航，并明确这是视角导航。
5. 完成视觉界面：标题、场景编号、返回入口、帮助或声音按钮、底部进度导航与暗色高对比面板。UI 不应盖住主要热点，触屏按钮至少保持易点击尺寸。
6. 使用 `code-implementation` 的最小修改原则把页面接入现有项目；优先复用现有构建系统和 Three.js 依赖。生成网页后检查资源路径、图片比例、窗口缩放和触摸事件。
7. 若用户要求分享，生成可离线打开的 HTML 或 ZIP；把脚本、样式和全景图内联，避免依赖网站根路径。常规浏览可离线运行；WebXR 头显模式需要 HTTPS 和支持 WebXR 的浏览器。

## 交付检查

- 页面打开后能看到全景画面，而不是黑屏或拉伸的平面图。
- 鼠标或手指拖动能连续环视，滚轮或双指操作不会把页面整体滚走。
- 点击热点能打开对应说明，底部场景按钮能更新导航状态。
- 全景图横向首尾衔接自然；UI 文本清晰且不覆盖核心内容。
- 离线包内没有仍指向 `/assets`、`/vendor` 或 `/image` 的必需资源路径。

详细的提示词结构、Three.js 场景骨架和离线打包注意事项见 [references/workflow.md](references/workflow.md)。

